# Shared

Cross-service contracts and reusable references live here.

- `contracts/`: OpenAPI and JSON schema artifacts
- `dto/`: shared data shape references
- `storage/`: object storage conventions

